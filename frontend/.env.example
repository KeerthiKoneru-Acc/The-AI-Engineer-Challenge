# Optional: absolute origin of the FastAPI app (uvicorn) for local dev, e.g.:
# NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
#
# If unset, the UI calls same-origin paths under /api (e.g. /api/chat/stream).
